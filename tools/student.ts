import { getDataFromMail } from "@/lib/email"
import { tool } from "ai"
import type { NeccessaryInfo } from "./index"
import { z } from "zod";

export const getStudentsTools = (neccessaryInfo?: NeccessaryInfo) => {
    return {
        
    }
}